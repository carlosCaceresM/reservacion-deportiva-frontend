export class Cancha {
  id: number;
  nombre: string;
  tipoCancha: string;
  tafira: number;

  constructor(id: number, nombre: string, tipoCancha: string, tafira: number){
      this.id = id;
      this.nombre = nombre;
      this.tipoCancha = tipoCancha;
      this.tafira - tafira;
  }
}
